# Azure TTS per-segment intermediate caches (rebuilt by generate-bedtime-audio.py)
audio/*/
# NOTE: audio/<id>.gz.mp3 are NOT ignored — they are the「光哥、阿築」narration
# set that index.html serves via the ?for=gz child selector (CHILDREN map).
# Secrets and local env
.env
# Local Claude Code settings
.claude/
# Python / macOS cruft
__pycache__/
*.pyc
.DS_Store
