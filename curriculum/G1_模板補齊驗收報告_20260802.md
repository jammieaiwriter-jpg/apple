# G1 模板補齊驗收報告（2026-08-02）

## Checkpoint

| Checkpoint | 結果 | 實際結果 |
|---|---|---|
| B0 Preflight | PASS | 24 templates、227 verified items、19 registry/JS types、scheduler 21/21。 |
| B1 校準批 | PASS | 新增 G1-06-01~04、G1-07-01~03；31 templates、297 items。 |
| B2 計算與數感 | PASS | 新增 G1-08、G1-10、G1-12、G1-13 共 22 技能；53 templates、509 items。 |
| B3 時間／長度／統計 | PASS | 新增 21 技能；unique skill coverage 71/79。 |
| B4 元件缺口 | PASS | `grid_tiling`、`solid_shape`、`life_scene` 與 8 技能模板完成；coverage 79/79。 |
| B5 最終重建 | FAIL（見 blocker） | JSON 可重現，但 HTML 連續建置 SHA 不同。 |

## 新增模板與覆蓋

新增 58 個模板：G1-05-01~05、G1-06-01~04、G1-07-01~03、G1-08-01~05、G1-09-02~06、G1-10-01~03、G1-11-01~05、G1-12-01~04、G1-13-01~10、G1-14-01~04、G1-15-01~04、G1-16-01~04、G1-17-01~02（每個檔名皆為 `{skillId}-a.json`）。

最終為 **82 模板檔，79/79 個 G1 skillId 有模板**。`G1-05-03` 與 `G1-09-04` 後續改以 `life_scene` 純 SVG 情境卡完成，不需外部素材。

## 最終驗證輸出摘要

執行：

```text
node tools/validate_figures.mjs
→ 檢查 82 個模板（57 個帶圖）、registry 22 型、figures.js 22 型
→ ✅ 三邊一致

python3 tools/matrix_gen/generate.py curriculum/templates --n 10 --seed 20260802
→ 模板數：82　題目總數：779（全部通過 verify，無靜默跳過）
→ 82 個模板均輸出 [OK]；無 AssertionError / TemplateError。

node tools/build_g1_school_html.mjs
→ Built math-g1-school-preview.html (1305 KB)
→ Templates: 82; verified items: 779; skills: 79

node --test tools/scheduler.test.mjs
→ tests 21 / pass 21 / fail 0
```

## 新增圖形元件與 SVG 結構檢查

- `grid_tiling`：規格為 `cols`、`rows`、`filled`（可選 `color`／`showGrid`）；4×3、filled=5 的 SVG 直接檢查為 13 個 `<rect>`（背景加 12 格），其中恰有 5 個 `#60a5fa` 填色格，`viewBox="0 0 232 180"`、`</svg>` 均存在。格子以 `r * cols + c` 排列，因此每格座標唯一、不重疊。
- `solid_shape`：規格 `shape`（可選 `color`）；cube SVG 直接檢查為 3 個 `<polygon>`（頂／左／右三個面），`viewBox="0 0 260 190"` 且閉合。正方體三面共享的頂點為 (138,105)，投影的三個面方向分別朝上、左下、右下。
- `life_scene`：規格 `scene`（可選 `focusShape`／`highlight`）；房子、窗戶、披薩、時鐘的框選場景及早餐、上學、午餐、睡覺共 8 組均檢查到 `viewBox="0 0 300 190"`、閉合 `</svg>`，且沒有 `<image>` 或 HTTP `href`。四個圖形辨識場景皆有高對比標示框。

## 兩次重建 SHA

第一次：

```text
generated_items.json  7f3ba13bcee93cf25e002cd1d1e8408bc5dc49cf57d0f9bbdfbe46eb39c0c2d5
math-g1-school-preview.html  38c56cbb2edc1c09f2efa10623017e282843adb8179fda850f593a4148dbaec2
```

第二次：

```text
generated_items.json  7f3ba13bcee93cf25e002cd1d1e8408bc5dc49cf57d0f9bbdfbe46eb39c0c2d5
math-g1-school-preview.html  daf66c4f654c5f692ba2c53bfdfeb3e657ea9fc84fef732ce4cef4b8b7247312
```

Blocker：`tools/build_g1_school_html.mjs` 第 92 行把 `new Date().toISOString()` 寫入 `build.builtAt`，使 HTML 每次建置不同。工單只允許修改第 541–543 行的 completed-skill 統計，故未更動 timestamp 寫法。

## 允許修改範圍與聲明

修改：`curriculum/templates/*.json`（上述 58 新檔）、`curriculum/figure-specs.json`、`tools/figures/figures.js`、`curriculum/FIGURE_BACKLOG.md`、建置產物 `tools/matrix_gen/output/generated_items.json`、`tools/matrix_gen/output/preview.html`、`math-g1-school-preview.html`、以及允許的一處 `tools/build_g1_school_html.mjs` 統計修正。本報告亦為工單要求的產物。

未修改 `curriculum/alignment/`、課綱 JSON、`progress.json`、`scheduler.js`、`index.html`；未安裝套件；未 commit、push、add、checkout、restore、reset 或 stash。
